# GitHub Frontend

1. Put these files in your GitHub Pages repo.
2. Edit `config.js` and paste your Apps Script Web App `/exec` URL.
3. Open the site.
4. Log in with:
   - admin / admin123

This frontend is intentionally simple and mobile-first so you can rebuild fast.
